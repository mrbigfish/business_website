/*
 * AppList Messages
 *
 * This contains all the text for the AppList component.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.components.AppList'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AppList component!',
  },
})
