/*
 * TriviaApp Messages
 *
 * This contains all the text for the TriviaApp container.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.containers.TriviaApp'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: ' ',
  },
})
